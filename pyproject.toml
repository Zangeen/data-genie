[project]
name = "folder"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Zangeen A", email = "zangeenahmad112@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "ipykernel>=7.3.0",
    "langchain>=1.3.15",
    "langchain-groq>=1.1.3",
    "panda>=0.3.1",
    "pandas>=3.0.5",
    "python-dotenv>=1.2.3",
    "streamlit>=1.61.1",
]

[project.scripts]
folder = "folder:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
